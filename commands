steps to perform operation :
git init
git status
git add .
git status
git commit -m "First Commit"
git remote add origin "URL of Repository App1"
git push origin master
   >username : github username "gayathrikota02"
go to github press on settings - delivery settings - personal access key -generate token - copy the token and paste in password
   >passsword: Copy token which generated by creating new token
now press on main and change to master and now you can see registration.html in First Commit
last again git push origin master
   >username
   >password (both same as above)
